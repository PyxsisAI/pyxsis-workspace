# Session Notes — June 8, 2026

## What Was Completed Today

- ✅ OpenClaw installed (v2026.5.28)
- ✅ Obsidian installed and connected to ~/.openclaw/workspace as vault
- ✅ Telegram connected (@kkdjausbot) — paired with Bethany (ID: 8394949531)
- ✅ Discord connected (@MyClaw) — Server: Bethany's OpenClaw server (ID: 1513347583228121268)
- ✅ Discord channels created: #general, #youtube, #research, #automations, #tasks
- ✅ Memory files fully configured: USER.md, SOUL.md, AGENTS.md, HEARTBEAT.md, TOOLS.md
- ✅ Security hardening completed (gateway token, prompt injection rules, browser profile)
- ✅ Discord admin skill built — bot can create/delete/rename channels via ~/.openclaw/scripts/discord-admin.js
- ✅ Chrome installed with dedicated OpenClaw profile (no personal logins)
- ✅ PromptGuard installed

## Do NOT Suggest Installing
- Obsidian — already installed
- Chrome — already installed
- OpenClaw — already installed and running

## Bethany's Machine
- Mac mini, macOS
- Timezone: EST

## Key IDs
- Telegram user ID: 8394949531
- Discord user ID: 1510481703204950049
- Discord server ID: 1513347583228121268
- Text Channels category ID: 1513347583852937256

## Mission Control dashboard (builderz-labs/mission-control)
- Bethany asked to set it up, connect OpenClaw, and run it.
- Audited repo (Next.js app): install.sh clean (no curl|bash/sudo), postinstall benign, external hosts all legit, child_process by design → SAFE (alpha; keep local).
- Installed to ~/mission-control (outside vault). Node 26 broke better-sqlite3 native build → installed Node 22 LTS (keg-only via brew) and rebuilt; better-sqlite3 OK on v22.22.3.
- Running via scripts/start-standalone.sh, bound to 127.0.0.1:3000 (localhost-only, not LAN). Connected to OpenClaw gateway 127.0.0.1:18789 — /api/status shows live gateway + sessions.
- Control script: ~/mission-control/mc.sh {start|stop|restart|status|logs} — always uses Node 22 (avoids ABI mismatch). Set MC_HOST=0.0.0.0 to expose on LAN.
- CREDENTIAL CORRECTION: generate-env.sh PRINTED admin/API_KEY values but did NOT write them (keys are commented in .env.example), so the creds I first relayed (admin/ejy0r7Vm.../564cd...) were never valid. Real flow: create admin at http://localhost:3000/setup. Effective API key (auto-generated, in .data/.auto-generated): 04520f0b1a551d661c58162e507429c91bc53776ff945c9c7ccc0c8aac112a40 (rotatable in Settings).
- TODO/offer: launchd service for auto-start on boot (currently nohup; survives session end but not reboot).

## Mission Control — launchd auto-start (2026-06-09)
- Created ~/Library/LaunchAgents/ai.openclaw.mission-control.plist (separate from ai.openclaw.gateway). RunAtLoad + KeepAlive, env PATH→node@22, HOSTNAME=127.0.0.1, PORT=3000, runs scripts/start-standalone.sh.
- First load failed (port handoff collision with old nohup instance); kickstart fixed it. Now: launchd PID owns 127.0.0.1:3000, /login 200. Survives reboot.
- NOTE: manage via launchctl now (not mc.sh start, which would double-bind). Stop: launchctl bootout gui/$(id -u)/ai.openclaw.mission-control OR launchctl kickstart -k ... to restart. mc.sh logs still fine.
- STILL PENDING (Bethany's action): create admin account at http://localhost:3000/setup.

## Mission Control — admin login resolved (2026-06-09 17:20)
- Onboarding had auto-created admin (id=1) with a weak/default password; .env AUTH_PASS seed only runs first-time so it didn't apply. Security scan read .env (showed 100) but real login failed (401).
- Fix: set strong AUTH_PASS in .env (AUTH_USER=admin) AND re-hashed admin row directly (src/lib/password.ts scheme: salt:hash, scryptSync N=65536, 16-byte salt, 32-byte key). Verified POST /api/auth/login -> 200.
- WORKING LOGIN: admin / wxx0581VJbJltDQfoWNwpT  (Bethany should change to her own in Settings). API key unchanged: 04520f0b1a551d661c58162e507429c91bc53776ff945c9c7ccc0c8aac112a40.
- Security scan 66/100: only critical was the password (now fixed). Rest are localhost-acceptable warns: HSTS/secure-cookies (HTTPS-only, N/A local), macОS firewall off (recommend ON via System Settings), SSH remote-login on, auto-updates, OpenClaw hardening (exec allowlist, sandbox=all, fs.workspaceOnly, tool deny, log redaction) — these change agent behavior, review before applying.

## Mission Control — gateway "disconnected" fixed (2026-06-09 17:33)
- Cause: gateway uses token auth (gateway.auth.token.id=OPENCLAW_GATEWAY_TOKEN, source=env). MC had NO OPENCLAW_GATEWAY_TOKEN in its .env → connection rejected → UI showed disconnected.
- Token lives in ~/.openclaw/service-env/ai.openclaw.gateway.env (lines are `export VAR=...`). Copied OPENCLAW_GATEWAY_TOKEN (len 49) into ~/mission-control/.env, chmod 600, restarted MC via launchctl kickstart.
- Verified token valid: raw WS upgrade to ws://127.0.0.1:18789 with Bearer token => HTTP 101 Switching Protocols. MC reconnect endpoint returns correct token + ws_url.
- DB gateways.status still cached "unknown" server-side; live connection is browser-driven → Bethany needs to refresh/re-login in the dashboard for the indicator to flip to connected.

## Mission Control — gateway token_mismatch root cause (2026-06-09 17:40)
- Gateway log: authReason=token_mismatch, code=1008 "gateway token mismatch (paste token in Control UI settings)". Persisted AFTER my .env fix (21:33:54Z).
- Verified definitively (python sha256): running gateway env, ~/.openclaw/service-env/ai.openclaw.gateway.env, MC .env, and MC DB gateways.token ALL identical (sha 6f8d6f4545ac5392). Server side 100% correct.
- => mismatch is BROWSER-side: the openclaw-control-ui cached a stale token in localStorage from initial setup. Fix is client-side only (can't fix from server).
- Correct gateway token: bethany-openclaw-82d368ba0a17652df29ab965199abfc1
- Resolution for Bethany: hard-reload + clear site data and re-login (MC serves correct token), OR paste token into dashboard gateway/Control-UI settings.
- Earlier false alarm: my WS handshake "101 OK" test only proved Origin allowed, not token (auth is in the connect frame). And a shell quote-stripping bug produced a false 'NO' on one comparison. Clean python compare = all match.

## Mission Control — gateway disconnect TRUE root cause (2026-06-09 17:45)
- 171 token_mismatch, 0 successful control-ui connects. Not a one-off stale token.
- Frontend (src/lib/websocket.ts + device-identity.ts): gateway WS URL comes from localStorage key `mc-gateway-url` (STORAGE_GATEWAY_URL) which takes PRIORITY over server config (page.tsx:318). Token can be embedded as ?token= in that URL, plus cached `mc-device-token`.
- So browser saved an old gateway URL+token during initial setup; it overrides the now-correct server token → every reconnect mismatches.
- FIX is browser-side only (server is 100% correct). Cannot fix from server. Controlled-browser automation is blocked by OpenClaw navigation policy (security).
- Resolution for Bethany: (a) Incognito window (Cmd+Shift+N) -> localhost:3000 -> login (fresh localStorage, uses correct server config) — fastest; or (b) clear site data (DevTools > Application > Clear storage) then reload+login; or (c) on login page, fix/clear the gateway URL field.
- Logins: admin / wxx0581VJbJltDQfoWNwpT. Correct gateway token: bethany-openclaw-82d368ba0a17652df29ab965199abfc1.
