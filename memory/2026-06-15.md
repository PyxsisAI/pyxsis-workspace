# 2026-06-15

## Gmail inbox cleanup (via `gog` CLI)
- Bethany asked to trash promo emails from DSW, "Carnival VIFP Club", MAC, TJMAXX across "all 3 gmails."
- `gog` (Google Workspace CLI, brew steipete/tap/gogcli) is installed + OAuth'd for 2 accounts: `bwilli787@gmail.com`, `zenithhealthsvcs@gmail.com`.
- Trashed in `bwilli787`: 28 (DSW + MAC Cosmetics + 1 T.J.Maxx), then 9 Shoe Carnival = 37.
- Trashed in `zenithhealthsvcs`: 3 Wayfair. **Total trashed: 40.** All recoverable in Gmail Trash 30 days.
- Caught ambiguity: "Carnival VIFP Club" (cruise line) had ZERO emails; only **Shoe Carnival** (shoe store) existed — confirmed with Bethany before trashing. No Carnival cruise mail anywhere.
- Trash workflow: `gog gmail search 'from:(domain)' --json --results-only` → extract ids → `gog gmail trash <ids> -y -a <acct>` → verify with `in:inbox` search.

## OPEN ITEM — 3rd Gmail not connected
- The "3rd gmail" is **bwtill757@gmail.com** — NOT yet authed to `gog`. Holds ~18 DSW emails Bethany still wants trashed.
- Tried to connect via `gog auth add bwtill757@gmail.com --services gmail --remote --step 1/2` (remote/manual flow: print URL → she approves → pastes 127.0.0.1 callback URL → step 2 exchanges).
- Could NOT auto-complete: sign-in needs her Google password+2FA (won't do); user Chrome profile attach failed (DevToolsActivePort missing — Chrome not running w/ remote debug).
- **Bethany said she'll tackle it later.** When ready: regen step-1 URL (old ones expire ~5–20m), she approves + pastes callback, run step 2, then trash 18 DSW in bwtill757.

## Earlier
- (2026-06-13) Built voice-note STT pipeline (afconvert → whisper-cli base.en). Still working.
