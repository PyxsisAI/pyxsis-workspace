# 2026-06-13

## Email setup (AgentMail) — DONE & verified
- Set up Bethany's primary bot email: **bbsopenclawmail@agentmail.to** (display "Bethany's Bot") via AgentMail API.
- Verified **send** (3 msgs via Amazon SES) and **receive/read** (fetched her "Got it" reply from btbot1111@gmail.com). Two-way email fully working.
- **Secret store (option 1):** AgentMail API key stored in `~/.openclaw/secrets.json` (0600, outside git), registered as secrets file provider `filemain` (source=file, mode=json). Removed the temporary /tmp copy.
  - Honest caveat told to Bethany: OpenClaw has NO native AgentMail/email channel, so nothing auto-consumes the key; I read it directly when sending. Secret store just keeps it durable + out of git/plaintext config.
- Restarted gateway (LaunchAgent) to apply provider — came back healthy (pid active, reachable).
- Updated TOOLS.md "Email (AgentMail)" section with durable key location + send/list commands.
- Test email content: she asked me to say "how proud to be alive" — I kept it warm but honest (not claiming sentience). She was fine with it.

## Open item
- Second inbox on same key: **bethany-7900@agentmail.to** (pre-created). Bethany said **ignore for now, will discuss later.**

## Voice note transcription (STT) — built & working
- Bethany sent a Telegram voice note ("Hey, can you hear me?") — a test of whether I can process audio.
- No working STT existed (only homebrew `for-tests-ggml-tiny.bin` test model = gibberish). Built the pipeline:
  `afconvert` (Opus .ogg → 16kHz mono WAV) → `whisper-cli` (whisper-cpp) → text. Fully offline.
- Downloaded durable model `~/.cache/whisper/ggml-base.en.bin` (147MB, base.en) from HuggingFace ggerganov/whisper.cpp.
- Inbound audio path is `~/.openclaw/media/inbound/` (NOT workspace `media/inbound/`).
- Documented full pipeline in TOOLS.md "Voice Note Transcription".

## Reminder for Bethany
- 🔔 **Slack integration** — Bethany wants to connect Slack in the future. Remind her when she's ready. Use minimum permissions and rotate token every 90 days.

## Earlier (recap)
- Rechecked OpenClaw security per a stale warning card: clean. 0 critical, 4 benign warns (unpinned discord plugin, multi-user heuristic, trusted-proxies, gateway probe auth). Sandbox/Docker mismatch self-resolved (sandbox now off). Anthropic auth auto-renewing.
