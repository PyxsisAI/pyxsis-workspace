# 2026-06-14

## Image generation task (01:40 EDT)
- Bethany wanted: image for "what is a skill" + a cat, then save as skill + push to GitHub.
- Gemini Nano-banana (`gemini-2.5-flash-image`) BLOCKED: key valid (text works) but image gen returns `free_tier_requests, limit: 0`. Needs billing-enabled Google project. Retrying does NOT help.
- Gemini key stored in `~/.openclaw/secrets.json` `/gemini/apiKey` (outside repo, 0600). Never commit.
- Pivoted to existing `svg-image-gen` skill (qlmanage + SVG, free, no API). Generated both images to `media/`.
- Committed + pushed to origin/main (PyxsisAI/pyxsis-workspace), commit 7f41c69.
- LESSON: For fast/free images default to svg-image-gen skill. Gemini images require billing.
